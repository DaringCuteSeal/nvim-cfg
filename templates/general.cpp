// created: 2025-10-18 12:01:04
// author: daringcuteseal
// tags: 
// source:

// til: 

// #include <dbg.h>
#include <bits/stdc++.h>

#define nl '\n'
#define fast_IO ios::sync_with_stdio(0); cin.tie(0);

using namespace std;

template <typename T>
using v = vector<T>;

typedef vector<int> vi;
typedef long long ll;

#ifdef ASSERTS
  #define dbg_assert(x) assert(x)
#else
  #define dbg_assert(x) ((void)0)
#endif

template <typename T>
T* cpp_malloc_bytes(size_t size) {
	return static_cast<T*>(::operator new(size));
}

template <typename T>
T* cpp_malloc_n(size_t n) {
	return static_cast<T*>(::operator new(sizeof(T) * n));
}

inline void cpp_free(void* ptr) noexcept {
	::operator delete(ptr);
}



int main() { fast_IO

	return 0;
}
